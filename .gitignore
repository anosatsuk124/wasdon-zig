# Zig build outputs / caches
.zig-cache/
zig-out/

# Auto-generated test fixture (produced by `zig build wasm-example` and
# consumed by the wasm parser and translator integration tests via @embedFile)
src/wasm/testdata/
src/translator/testdata/

# Rust build outputs (produced by `cargo build` for the examples/*-rs crates)
target/

